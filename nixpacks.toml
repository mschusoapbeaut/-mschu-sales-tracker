[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["echo 'Standalone server - no build needed'"]

[start]
cmd = "npx tsx server/standalone-server-combined.ts"
