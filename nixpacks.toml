[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]
[phases.install]
cmds = ["pnpm install"]
[phases.build]
cmds = ["echo 'Skipping expo export - standalone server generates HTML'"]
[start]
cmd = "npx tsx server/standalone-server.ts"
